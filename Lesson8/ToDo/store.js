
/*
window.onload =function(){ //ensures the page is loaded before functions are executed.
    document.getElementById("addRecord").onclick = store;
}

export function store(){ //stores items in the localStorage
    var brand = "task name";
    var price ="2000";
    var key = "1";

    const car = {
        brand: brand,
        price: price,
    }

    window.localStorage.setItem(key,JSON.stringify(car));  
    //converting object to string
}
*/